query getProject($projectId: ID!) {
  node(id: $projectId) {
    ... on Project {
      id
      name
      issues {
        id
        createdAt
        updatedAt
        summary
        description
        number
      }
      boards {
        id
        createdAt
        updatedAt
        name
      }
    }
  }
}
